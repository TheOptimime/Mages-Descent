using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class CSVToAttack : MonoBehaviour {

    

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        FileStream fs;
        //File.AppendAllText("/Assets/Data" + "/Assets/Data/AttackData.csv", "," + rollNoInputField.text + fieldSeperator + nameInputField.text);

    }
}
